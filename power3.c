#include<stdio.h>
#include <math.h>

int n = 26;

int main()
{
    int k;
    int j = 3;
    for(int x = 0; k <= n; x++)
    {
        k=pow(j,i);
        if(k==n){
            printf("true");
            break;
        }
        else if(k > n)
        {
            printf("false");
        }
    }
    return 0;
}